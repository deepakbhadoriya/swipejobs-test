// ? Enable this when testing locally
// const testUrl = 'localhost:3000'

// ? Enable this when testing production Server
const testUrl = 'https://swipejobs-test.vercel.app/'

export default testUrl
